#!/usr/bin/env ruby

require 'tomdoc'

if ARGV.delete('-p')
  require 'pp'
  pp TomDoc::Parser.parse(ARGF.read)
elsif ARGV.delete('-s')
  require 'pp'
  pp RubyParser.new.parse(ARGF.read)
else
  TomDoc::Generators::Console.generate(ARGF.read)
end
